import { tv } from "tailwind-variants";

export const avatarVariants = tv({
  base: `rounded-full w-45 m-2
         border-6 border-solid border-black dark:border-white
         overflow-hidden
         md:block hidden`,
});